name: example
description: A new Flutter project.

publish_to: "none"

version: 0.0.10+4

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  vs_media_picker:
    path: ../
  provider: ^6.1.1
  photo_view: ^0.14.0
  better_video_player: ^1.2.8
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
